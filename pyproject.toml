[project]
name = "mestrerpg-ia"
version = "1.0.0"
description = "Motor de RPG de mesa com narração por Inteligência Artificial, construído com Python + Pygame."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "groq>=0.4.0",
    "pygame>=2.6.0",
    "python-dotenv>=1.0.0",
]
